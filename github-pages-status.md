# GitHub Pages status

- The custom user URL `https://demro-labs.github.io/` returned GitHub Pages 404 because this repository is a project site, not a `demro-labs.github.io` user-site repository.
- The project URL is `https://demro-labs.github.io/punks-base-collection/`.
- The Pages API reports `build_type: workflow`, `public: true`, and the project URL.
- Run `33107529167` completed successfully: build and deploy jobs both succeeded, including upload and deployment.
- A fresh browser check is still required after the successful run because the earlier project URL check occurred before that deployment was available or before propagation completed.
